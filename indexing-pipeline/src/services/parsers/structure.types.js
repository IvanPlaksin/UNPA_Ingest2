/**
 * @typedef {Object} Atom
 * @property {string} id - UUID of the atom
 * @property {'paragraph' | 'email_message' | 'code_block'} type - Type of the content
 * @property {string} content - The actual text content
 * @property {string} context - Parent context (e.g., "Header: Requirements")
 * @property {Object} metadata - Additional metadata (page number, author, date, etc.)
 */

module.exports = {};
