/**
 * Temporal Domain Module (D4)
 *
 * State machines, lifecycle states, and transitions.
 */

const {
  TemporalDomainService,
  StateType,
  TriggerType,
} = require('./temporal-domain.service');

module.exports = {
  TemporalDomainService,
  StateType,
  TriggerType,
};
