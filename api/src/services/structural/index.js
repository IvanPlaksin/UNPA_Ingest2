/**
 * Structural Domain Module (D1)
 *
 * Tables, columns, foreign keys, and entity classification.
 */

const {
  StructuralDomainService,
  StructuralEntityType,
} = require('./structural-domain.service');

module.exports = {
  StructuralDomainService,
  StructuralEntityType,
};
